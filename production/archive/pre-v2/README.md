# V2 前历史归档（非生产）

这里保存为审计而保留的迁移前草稿、提示词和候选文件。该目录不属于 `production/midjourney/` 的生产入口，也不得作为当前视觉、身份、服装、地点、道具或参数基线。

当前可执行的视觉合同、资产绑定和 Midjourney 8.2 命令只位于：

- `production/style/`
- `production/midjourney/v2/`
- `production/midjourney/v2-asset-prompt-catalog.json`

历史资料只能在明确标记为审计/迁移的工作中读取，不能被编译器或生成流程自动引用。
